import React from "react";
import { Alert } from "@mui/material";

const EmptyDisplay = (): JSX.Element => (
  <Alert severity="info">Select an option to view data</Alert>
);

export default EmptyDisplay;
